import React from "react";
import Box from "../../atoms/Box";
import Base from "../../organisms/Base";

const Shop = () => {
	return (
		<Base>
			<Box type="rowFlex"></Box>
		</Base>
	);
};

export default Shop;
